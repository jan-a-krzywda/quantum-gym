calibration-gym
===============

Folder intended to contain ML pipelines and experiments that focus on calibration,
VAE training, and latent-space models. Move your `ML/` and `quantum_code/` pieces
here when ready.

Note: This file is a placeholder; move code with `git mv` to preserve history.

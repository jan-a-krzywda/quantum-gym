preparation-gym
===============

Tools and scripts for ansatz construction, circuit preparation, and beam search
utilities. Useful for separating circuit-design logic from ML and shadow tooling.

Note: placeholder — use `git mv` to move existing scripts and preserve history.
